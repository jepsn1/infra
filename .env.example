# Real file lives at compose/.env (generated by setup.sh, chmod 600, never committed).

# Password for the shared Postgres superuser (postgres)
POSTGRES_PASSWORD=

# Email for Let's Encrypt certificate notifications (optional)
CADDY_EMAIL=
